# Wanted images for project sub-pages

Nexus already has three macOS screenshots (captured 2026-08-02). These three
are still wanted from Josh; drop them in `public/screenshots/<slug>/` and add
entries to the matching `images` array in `src/content/projects-detail.ts`.

- **GhostPane** (`public/screenshots/ghostpane/`): a GhostPane terminal window
  on Windows showing the Ghostty-styled theme, ideally next to the config that
  produced it. Suggested ~1200px wide, jpg/webp under 250 KB.
- **GlassForge** (`public/screenshots/glassforge/`): the customizer UI over a
  desktop showing wallpaper-adaptive glass. Same sizing.
- **Home lab** (`public/screenshots/home-lab/`): the Grafana homelab dashboard
  or a tidy rack photo. Same sizing. Crop out any credentials, tokens, or
  private URLs before adding.
